---
name: 任意 GVR 支援
overview: 把 exporter 從固定 Kind 白名單擴展成 YAML 可宣告任意 Kubernetes GVR，讓未來新增 CronJob、Job、Ingress、CRD 只需改 config/RBAC YAML 與文件，不再改 Go 程式。計畫會保留 legacy catalog 以相容舊 config 中的 kind-only 條目，但移除隱式全 watch；`watch.resources` 未指定時預設完全不 watch。另加入預設關閉的 discovery 輔助模式；若 discovery 未開或無權限且 YAML 未寫完整 GVR，會以明確錯誤失敗。
todos:
  - id: resource-schema
    content: 設計並實作任意 GVR resource descriptor 與 runtime registry
    status: completed
  - id: no-default-watch
    content: 將未指定 watch.resources 的預設行為改為完全不 watch，並補遷移錯誤訊息
    status: completed
  - id: discovery-opt-in
    content: 加入預設關閉的 discovery 輔助解析模式與權限/失敗處理
    status: pending
  - id: config-validation
    content: 補齊 schema、source、duplicate、scope、legacy kind-only 驗證
    status: completed
  - id: dynamic-informers
    content: 將 informer/lister/cache key 改為 resource name + GVR registry
    status: completed
  - id: owner-resolution
    content: 用 ownerReference GVK 對應任意 watched resource，並設定化 topController
    status: completed
  - id: errors-logging
    content: 改善 config、LIST/WATCH、RBAC、owner miss 的錯誤訊息與 log
    status: completed
  - id: docs-rbac
    content: 更新 README、docs/CONFIG.md、docs/INTEGRATION_TESTS.md、部署 manifest 註解與 RBAC 文件
    status: completed
  - id: tests-e2e
    content: 新增 unit、collector、integration/e2e 覆蓋 CronJob/Job/Ingress/CRD 與 scope/RBAC
    status: completed
  - id: load-analysis
    content: 檢查 apiserver 負擔、watch 數量、event payload、CPU/memory 與 leak 風險
    status: completed
isProject: false
---

# 任意 GVR 支援計畫

## 目標與設計邊界

- 目標：未來新增 `CronJob`、`Job`、`Ingress`、任意 CRD 時，只在 YAML 宣告 GVR/GVK/scope/rules，Go 程式不再新增 registry entry 或 switch 分支。
- 保留相容性：既有 `watch.resources[].kind: Pod`、`rules[].anchor: Pod`、`source: Deployment` 繼續可用，內建資源可由 legacy kind-only 寫法補齊 GVR。
- 預設最小負擔：`watch.resources` 未指定或為空時，effective watch set 為空，不再隱式 watch 全部內建資源；如果 rules 引用未 watch 的 anchor/source，啟動時明確失敗並提示補上 `watch.resources`。
- Discovery 輔助模式：新增 opt-in 設定，預設不啟用；啟用後可用 `apiVersion + kind` 由 apiserver discovery 補出 `resource` 與 `scope`。
- 新能力：新 YAML resource descriptor 可完整描述 `name`、`group`、`version`、`resource`、`kind`、`scope`、namespace/selector、以及 owner-chain/top-controller 語意；未啟用 discovery 時，非 legacy kind-only resource 必須寫完整 GVR。
- 非目標：不自動產生或套用 RBAC，因 exporter 不應修改叢集權限；改為提供清楚文件、範例與錯誤訊息。

## 建議 YAML Schema

新增「資源識別名稱」作為 rules/source 的穩定引用鍵，避免同一個 `kind` 出現在不同 API group/version 時衝突：

```yaml
watch:
  resources:
    - name: CronJob              # rules[].anchor / labels[].source 使用的邏輯名稱；省略時預設為 kind（僅限不衝突）
      group: batch
      version: v1
      resource: cronjobs         # plural resource，RBAC 也用這個
      kind: CronJob              # ownerReferences 用 GVK 對應 cache
      scope: Namespaced          # Namespaced 或 Cluster
      namespaces: [prod]
      labelSelector: "app=my-app"
      fieldSelector: "metadata.name=my-cronjob"
      owner:
        follow: true             # 預設 true；允許 resolver 沿 ownerReferences 找此 resource
        topController: false     # 取代目前硬編碼 Deployment/StatefulSet/DaemonSet

rules:
  - name: cronjob_info
    anchor: CronJob
    labels:
      cronjob:
        path: metadata.name
```

若省略 `watch.resources` 或給空陣列，預設為「不建立任何 informer」：

```yaml
watch: {}
```

這會使有效 watch set 為空；若 `rules` 還有任何 `anchor` 或顯式 resource `source`，驗證會 fail fast，避免部署後安靜地輸出空 metrics。需要收集哪些資源就必須在 YAML 明確列出，這是降低 apiserver 負擔的主要設計。

CRD 範例：

```yaml
watch:
  resources:
    - name: Widget
      group: example.com
      version: v1alpha1
      resource: widgets
      kind: Widget
      scope: Namespaced
```

Discovery 輔助模式預設關閉。啟用後允許只寫 `apiVersion + kind`，由叢集 discovery 在啟動時解析 `resource` 與 `scope`：

```yaml
discovery:
  enabled: true

watch:
  resources:
    - name: CronJob
      apiVersion: batch/v1
      kind: CronJob
```

未啟用 discovery 或 discovery 沒權限時，這種不完整 descriptor 會啟動失敗，錯誤需明確提示補上完整 GVR：

```yaml
watch:
  resources:
    - name: CronJob
      group: batch
      version: v1
      resource: cronjobs
      kind: CronJob
      scope: Namespaced
```

資料流：

```mermaid
flowchart LR
  configYaml["Config YAML"] --> validate["config.Validate"]
  validate --> discoveryOptIn["Discovery Resolver (opt-in)"]
  discoveryOptIn --> registry["Runtime Resource Registry"]
  registry --> informers["Dynamic Informers by GVR"]
  informers --> cache["Cache keyed by resource name"]
  cache --> resolver["Owner Resolver maps GVK to resource name"]
  resolver --> collector["Collector evaluates rules and labels"]
```

## 實作步驟

1. 重塑 config 資源模型於 [pkg/config/config.go](pkg/config/config.go)。
   - 將 `ResourceInfo` / `WatchResource` 擴成可由 YAML 提供 GVR/GVK/scope 的 descriptor。
   - 新增 `name` 作為 canonical resource key；`kind` 只代表 Kubernetes GVK kind。
   - 建立 runtime registry：`byName`、`byKindIfUnique`、`byGVK`、`byGVR`。
   - 保留內建 legacy catalog 僅用於舊的 kind-only watch entries，不再用於「未指定時全 watch」；catalog 是我們程式內的相容層，不是 client-go 提供的完整目錄。
   - 修改 `WatchScope.EffectiveResources()`：省略或空的 `watch.resources` 回傳空集合；移除目前由 `allSupportedKindOrder` 展開全部資源的行為。

2. 加入 discovery 輔助解析，預設關閉。
   - 新增頂層設定，例如 `discovery.enabled: false` 作為預設值。
   - 啟用時，允許 `apiVersion + kind` descriptor 省略 `group/version/resource/scope` 中可由 discovery 推導的欄位。
   - 啟動時透過 Kubernetes discovery 查詢實際 cluster 的 APIResourceList，解析 `apiVersion + kind -> group/version/resource/scope/verbs`。
   - 若 discovery API 無權限、不可用、查不到、或找到多個候選 resource，啟動失敗並提示改寫完整 GVR。
   - discovery 只用來補 schema 與 scope，不取代 `get/list/watch` RBAC；LIST/WATCH 權限仍由後續 dry-run/informer 建立驗證。

3. 擴充驗證規則於 [pkg/config/config.go](pkg/config/config.go) 與 [pkg/config/config_test.go](pkg/config/config_test.go)。
   - 驗證 `name/group/version/resource/kind/scope` 必填與格式，`resource` 必須是 lowercase plural DNS-ish 字串，`version` 不可空。
   - 驗證 duplicate `name`、duplicate GVR、duplicate GVK、同 kind alias 衝突。
   - 驗證 `scope=Cluster` 不可設定 `namespaces`；`scope=Namespaced` 可選擇 cluster-wide watch 或 namespace-scoped watch。
   - 驗證 `rules[].anchor`、`labels[].source`、`fallbacks[].source`、`expandLabels[].source` 只能引用 built-ins 或 registry 裡的 `name` / 唯一 kind alias。
   - 驗證 `watch.resources` 省略或空時，任何 rule anchor 或 resource source 都會報錯：提示「預設不 watch；請明確加入所需 watch resource」。
   - 對 non-legacy 的不完整 descriptor 給明確錯誤：若 `discovery.enabled=false`，提示「請補完整 `group/version/resource/kind/scope` 或啟用 discovery」；若 discovery 失敗，提示 discovery 權限/API 問題與完整 GVR fallback 範例。

4. 改造 informer/lister 查表於 [pkg/collector/listers.go](pkg/collector/listers.go)。
   - 將 `ScopedInformers` 的 key 從 kind string 改成 resource name，並從 registry 取得 GVR/scope。
   - `DryRunSelectors`、`Get`、`ListAll`、`HasKind/WatchedKinds` 改為 resource-name 語意，錯誤訊息包含 `name`、`gvr`、`scope`、`namespace`。
   - 對 LIST/WATCH RBAC 失敗、API resource 不存在、selector 不合法，保留原始 Kubernetes error 並包上設定位置。

5. 改造 owner resolver 於 [pkg/collector/resolver.go](pkg/collector/resolver.go)。
   - resolver 接收 registry，用 ownerReference 的 `apiVersion + kind` 找到已 watch 的 resource name，而不是只看 `kind` 白名單。
   - Chain 同時放入 `anchor`、`ownerController`、`topController`、resource `name`；只有 kind alias 唯一時才額外放 `Kind`，避免 source 歧義。
   - `topControllerKinds` 改為 config-driven，例如 `owner.topController: true`，內建 Deployment/StatefulSet/DaemonSet 以 legacy default 保持現行行為。
   - 未 watch 的 owner GVK 不視為 fatal，但 warning 要包含 owner GVK、anchor、namespace/name 與建議補 watch resource。

6. 同步 collector 與 CLI log 於 [pkg/collector/collector.go](pkg/collector/collector.go) 和 [cmd/main.go](cmd/main.go)。
   - `rule.Anchor` 解析成 resource name 後再 scrape `ListAll`。
   - self metrics 的 `kind` label 可改名或補 `resource` label需謹慎；若維持既有 metrics，至少以 resource name 輸出，文件說明行為。
   - 啟動 log 顯示完整 watch resources：`name/group/version/resource/kind/scope/namespaces/selectors`。

7. 更新部署與 RBAC 文件。
   - [docs/CONFIG.md](docs/CONFIG.md) 更新 schema、範例、legacy migration、source/anchor 規則、scope 檢查、fieldSelector 說明。
   - [docs/INTEGRATION_TESTS.md](docs/INTEGRATION_TESTS.md) 更新整合測試說明，涵蓋 empty watch、任意 GVR、discovery opt-in、CRD、RBAC 缺權限與 apiserver watch delta 驗證方式。
   - [README.md](README.md) 將「支援固定八種資源」改成「內建範例 + 任意 GVR」，並明確說明不再有隱式全 watch。
   - [deploy/manifests.yaml](deploy/manifests.yaml) 保留內建資源 RBAC，範例 config 必須明確列出要 watch 的 resources，並新增註解或範例段落說明 CronJob/Job/Ingress/CRD 需手動加入 `apiGroups/resources/get/list/watch`。
   - 文件明確說明 discovery RBAC：若啟用 discovery，ServiceAccount 需要能讀 API discovery endpoint；但這仍不代表已具備目標 GVR 的 `get/list/watch`。
   - 完成實作後必須同步檢查 [docs/CONFIG.md](docs/CONFIG.md) 與 [docs/INTEGRATION_TESTS.md](docs/INTEGRATION_TESTS.md)，避免文件仍描述舊的固定 kind 或隱式全 watch 行為。

8. 補測試與範例。
   - Unit：config parse/validate、legacy compatibility、未指定 `watch.resources` 不建立任何 watch、任意 GVR、discovery disabled incomplete descriptor、discovery success/failure、duplicate/collision、scope/namespace、source resolution、owner GVK mapping。
   - Collector：fake dynamic client 建立 custom resource informer，驗證 `ListAll`、`source`、owner chain、topController。
   - E2E：namespaced CRD + CR、cluster-scoped CRD + CR、CronJob/Job/Ingress 範例 config、缺 RBAC 時 startup error/log、以及未指定 `watch.resources` 時不產生 apiserver watch connection。
   - 文件範例要能被 `config.Load/Validate` 或測試 helper parse，避免 schema 漂移。

## Apiserver 負擔檢查

- 預設負擔：`watch.resources` 未指定時不建立 informer，因此 discovery disabled 時預設 apiserver 額外負擔為 0 個 discovery call、0 個 LIST、0 個 WATCH connection。若 config 有 rules 但未列 watch resources，啟動會 fail fast，不進入半運作狀態。
- Discovery 負擔：`discovery.enabled=false` 時完全不 call discovery。啟用時只在啟動或未來 config reload 時解析一次，不在 scrape 或 watch event 時呼叫；查詢範圍應限於 YAML 中缺 GVR 的 `apiVersion`，避免全叢集 discovery。
- Watch connection 數量：每個 resource 的 watch 數量應為 `1`（cluster-wide namespaced watch 或 cluster-scoped watch）或 `len(namespaces)`（namespaced resource 明確指定 namespaces）。總量約為 `sum(resourceWatchScopes)`，不再因 legacy default 自動包含所有內建資源。
- 每個 watch 監控的 resource 量：由 apiserver selector 決定，目標是要求文件與範例鼓勵使用 `namespaces`、`labelSelector`、`fieldSelector` 縮小 LIST 初始物件量與後續 event payload。event packet size 主要隨該 GVR 物件大小與 selector 後事件數變動，程式端不應再額外 watch parent resource，除非 rules/source/owner chain 需要。
- 啟動 LIST 負擔：每個 informer 啟動會有初始 LIST；有 selector 時另有 dry-run LIST 驗證。需確認 dry-run 只對設定 selector 的 resource 執行，並使用 `Limit: 1`，避免放大啟動壓力。
- Owner chain 負擔：owner 解析只能讀本機 informer cache，不因 owner miss 額外打 apiserver。需要 parent 資訊時使用者必須明確 watch parent resource，這會增加 watch 數量但保持可預期。
- 目標驗證：更新 topology/e2e 測試，量測 `apiserver_longrunning_requests{verb=\"WATCH\",resource=...}` delta，覆蓋 empty watch、cluster-wide、per-namespace、kind subset、CRD cases。

## CPU、Memory 與 Leak 檢查

- Memory 主要來自 informer cache，估算約與「被 watch 的物件數 × unstructured object 大小 × informer/indexer overhead」成正比。任意 GVR 後需避免預設全 watch，並鼓勵 selector/namespace 限縮以降低 cache 常駐物件數。
- CPU 主要來自三段：client-go decode/store watch events、scrape 時遍歷 anchor cache 與 path evaluation、dynamic labels union/sort。任意 GVR 不應引入背景輪詢；scrape CPU 與每條 rule 的 anchor 數、`forEach` 展開數、`expandLabels` key 數成正比。
- Leak 風險重點是 informer lifecycle、registry/cache map、scrape 暫存資料。計畫要求所有 informer 只由 `Start(ctx)` 管理並跟隨 ctx 停止；不做定期 discovery goroutine；scrape 的 `pendingSeries`、dynamic map、source maps 不保留到下一次 scrape。
- Dynamic label cardinality 可能造成短時間 CPU/memory 尖峰，應保留 `maxKeys`、`allow`、`deny` 文件建議，並在測試中覆蓋大 label map 不會跨 scrape 留存。
- 測試/觀測：新增或更新 benchmark，涵蓋多 GVR、多 namespace、CRD unstructured、大量 anchors、`expandLabels` 高 key 數；用 Go race/leak 類檢查或長時間 idle e2e 驗證 goroutine/watch connection 不持續增加。

## 錯誤處理要求

- Config error 應指出 YAML 路徑，例如 `watch.resources[2].resource`、`rules[0].labels["foo"].source`。
- `watch.resources` 未指定或空、但 rules 需要 anchor/source 時，錯誤應提示「預設不 watch；請明確加入 watch.resources」，並列出缺少的 resource names。
- 不完整 GVR 且 discovery 未啟用時，錯誤應直接指出 `discovery.enabled=false`，並提示補 `group/version/resource/kind/scope`。
- discovery 啟用但無權限或查詢失敗時，錯誤應指出 discovery 失敗原因，並提示可關閉 discovery 後改寫完整 GVR。
- API discovery/list/watch error 應指出 GVR、scope、namespace 與 selector。
- source 歧義要 fail fast，例如兩個 resource 都是 `kind: Widget` 且 rule 使用 `source: Widget`，要求改用 `source: <name>`。
- 缺 RBAC 或 API resource 不存在要在啟動時失敗，而不是等到 scrape 時靜默空資料。
- owner chain miss 不應讓 scrape 失敗，但要有節流後的 warning 與可操作建議。

## 風險與注意事項

- 任意 GVR 會讓錯誤主要來自使用者 YAML 與叢集 RBAC/API discovery，錯誤訊息品質很重要。
- 移除隱式全 watch 是降低 apiserver 負擔的刻意行為，但對依賴舊預設的 config 是 breaking change；文件與錯誤訊息必須給明確 migration path。
- Discovery 是 opt-in，避免預設啟動就依賴 discovery 權限或 live cluster 查詢，並保留離線 config 驗證的可預期性。
- `topController` 從硬編碼改為設定後，舊設定必須維持 Pod -> ReplicaSet -> Deployment 的現有結果。
- 同 kind 不同 group 是 CRD 常見情境，必須用 `name` 消除歧義。
- cluster-scoped resource 使用 namespace-scoped informer 會錯，要在 Validate 或 startup discovery 階段攔截。
- fieldSelector 無法可靠靜態白名單化到任意 CRD，應以 startup dry-run LIST 驗證為準。