apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  labels:
    app.kubernetes.io/name: clickhouse-integration
spec:
  selector:
    app.kubernetes.io/name: clickhouse-integration
  ports:
    - name: native
      port: 9000
      targetPort: native
      protocol: TCP
    - name: http
      port: 8123
      targetPort: http
      protocol: TCP
